@model IEnumerable<EmployeeCostCalculator.Models.Dependent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Employee First Name
        </th>
        <th>
            Employee Last Name
        </th>
        <th>
            Dependent First Name
        </th>
        <th>
            Dependent Last Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostOfBenefits)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostOfBenefits)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DependentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DependentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DependentId })
        </td>
    </tr>
}

</table>
